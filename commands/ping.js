module.exports = {
    name: 'ping',
    aliases: ['ping1', 'ping2'],
    cooldown: 0,
    description: 'Testing',
    async execute(message, args, cmd, client, Discord){
        //If the user has used the play command
        if (cmd === 'ping'){
            
        }
    }
    
}